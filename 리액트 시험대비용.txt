1. 불러오기
INDEX - MAIN - APP
(IMPORT 포함)

2.
js 개념 안 된다.
=> 배열.push 안 됨.

set함수(인자); // 인자에 값을 넣어

3. return 값을 빼면 안 돼

4. 함수 이름은 똑같이 써야겠지?

5. 이름을 확인 잘 할 것. 배열 객체 속성 등에서도 마찬가지.

*** props가 무엇인가?
props.todoList 등 요소 확인 잘 하기

6. onClick 등 잘 하기. const 등